---
import Brand from "../components/Brand.astro";
const title = "Agentic Assembly — Sign";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700;800&family=Noto+Sans:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --ink: #0e0d0c;
        --ink-dim: #4a463e;
        --accent: oklch(0.7 0.2 130);
        --mono: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
        --sans: "Noto Sans", system-ui, sans-serif;
      }

      html,
      body {
        margin: 0;
        padding: 0;
        background: #f3efe4;
        color: var(--ink);
        font-family: var(--sans);
        -webkit-font-smoothing: antialiased;
      }

      .sheet {
        width: 297mm;
        height: 210mm;
        margin: 0 auto;
        background: #ffffff;
        padding: 18mm 22mm;
        box-sizing: border-box;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: space-between;
        text-align: center;
        box-shadow: 0 8px 40px rgba(0, 0, 0, 0.12);
      }

      .middle {
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .info {
        text-align: center;
      }

      .info__time {
        font-family: var(--mono);
        font-size: 22mm;
        font-weight: 700;
        line-height: 1;
        letter-spacing: 0.01em;
        white-space: nowrap;
      }

      .info__where {
        font-family: var(--sans);
        font-size: 10mm;
        font-weight: 400;
        margin-top: 6mm;
        white-space: nowrap;
        color: var(--ink);
        letter-spacing: 0.01em;
      }

      .info__where strong {
        font-weight: 700;
      }

      .footline {
        font-family: var(--sans);
        font-size: 6.5mm;
        font-weight: 500;
        letter-spacing: 0.04em;
        color: var(--ink);
        text-align: center;
        white-space: nowrap;
      }

      .footline em {
        font-style: normal;
        color: var(--accent);
      }

      @page {
        size: A4 landscape;
        margin: 0;
      }

      @media print {
        html,
        body {
          background: #ffffff;
        }
        .sheet {
          box-shadow: none;
          margin: 0;
        }
      }
    </style>
  </head>
  <body>
    <div class="sheet">
      <Brand size="28mm" />

      <div class="middle">
        <div class="info">
          <div class="info__time">10:00 — 12:00</div>
          <div class="info__where">
            Join us inside <strong>The DECK</strong> every other Saturday
          </div>
        </div>
      </div>

      <div class="footline">
        RSVP Requested — find us on <em>meetup.com</em> — <em>a13y.org</em>
      </div>
    </div>
  </body>
</html>
